#!/bin/bash -e

if [[ "$*" == *"./bin/rails server"* ]]; then
  echo "==> Preparing primary database..."
  ./bin/rails db:prepare

  echo "==> Preparing Solid databases..."
  ./bin/rails solid:prepare_databases
fi

exec "$@"